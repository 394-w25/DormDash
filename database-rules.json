{
    "rules": {
        "users": {
            ".read": "auth != null",
            "$user_id": {
                "requests": {
                    "$request_id": {
                        ".write": "(auth != null) && ($user_id === auth.uid)"
                    }
                }
            }
        }
    }
}
